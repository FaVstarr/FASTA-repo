import { View, Text, SafeAreaView } from 'react-native'
import React from 'react'
import { Paystack } from "react-native-paystack-webview";

export default function DeliveriesPayment() {
  return (
    <SafeAreaView>
      <Text>DeliveriesPayment</Text>
    </SafeAreaView>
  )
}